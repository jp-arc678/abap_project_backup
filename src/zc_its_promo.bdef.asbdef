projection;
strict ( 2 );
use draft;

define behavior for ZC_ITS_PROMO alias Promotion
{
  use create;
  use update;
  // no delete - see the base behaviour definition

  use action Activate;
  use action Discard;
  use action Edit;
  use action Resume;
  use action Prepare;
}